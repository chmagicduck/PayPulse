/**
 * 预留给后续 PRD 校验功能的入口。
 *
 * 规划职责：
 * - 检查 `docs/prd/<version>/README.md` 是否存在
 * - 检查版本目录是否包含 `overview.md`、`domain-model.md`、`data-model.md`、`page-map.md`、`acceptance.md`
 * - 检查 `pages/*.md` 数量是否与 `docs/prototype/<version>/` 中的页面原型数量一致
 * - 检查页面文档里的 route / prototype 是否能映射到平铺页面路径
 * - 检查旧的 `docs/prd/feature-*` 目录是否已移除
 */

export {}
