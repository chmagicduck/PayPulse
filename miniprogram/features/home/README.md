# home

## 目标

承载当前仓库的占位首页，用于验证新的架构骨架和默认路由。

## 页面

- `dashboard`

## Model 文件

- `model/types.ts`
- `model/schema.ts`
- `model/state.ts`
- `model/actions.ts`
- `model/storage.ts`

## 说明

当前只放工程骨架，不放业务逻辑。后续应以 PRD 为准继续实现。
